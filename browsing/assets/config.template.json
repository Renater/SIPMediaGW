{
    "lang": "${IVR_LANG}",
    "min_ivr_digit_length": "${IVR_DIGIT_LENGTH}",
    "webrtc_domain": "${WEBRTC_DOMAIN}",
    "conf_mapper": {
        "url": "${CONFMAPPER}"
    },
    "auth_token": {
        "jwt": "${AUTH_TOKEN}",
        "url": "${TOKEN_API_URL}",
        "secure_room_regexp": "${ROOM_REGEXP}"
    }
}