# Order in which the schema files must be applied.
#
# Alphabetical order is not dependency order: schema_reporting_lot1.sql reads
# functions that schema_reporting_lot0.sql defines, and schema_org_units.sql
# adds a column to calls that schema.sql creates. The order is written down
# here rather than inferred, and tools/migrate.sh reads this file.
#
# A test checks that every .sql file appears here, so a new one cannot be added
# without saying where it goes.

schema.sql
schema_org_units.sql
schema_pool.sql
schema_reporting_lot0.sql
schema_reporting_lot1.sql
schema_reporting_lot2.sql
schema_reasons.sql
schema_users.sql

# Not part of the schema, and deliberately not applied by the loop above:
#   bootstrap.sql                        roles and database, run once by hand
#   drop_gateway_hours.sql               one-off removal, run once per deployment
#   drop_dead_objects.sql                one-off removal, run once per deployment
#   drop_rates.sql                       one-off removal, run once per deployment
#   org_units.sample.sql                 example rules, never for production
