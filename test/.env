HOST_IP=192.168.92.1

# STUN server variables (see .env_turn for credentials)
STUN_SRV=${HOST_IP}

# Kamailio variables (see .env_kamailio for credentials)
SIP_DOMAIN=${HOST_IP}
DEBUG_LEVEL=0 # 0 (warning), 1 (notice), 2 (info), 3 (debug)
ANTIFLOOD=True
TLS=True
LOCAL_IP=${HOST_IP}
PUBLIC_IP=${HOST_IP}
